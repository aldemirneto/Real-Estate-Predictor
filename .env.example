# Copy this file to .env and fill in your values.
# .env is git-ignored — never commit real credentials.

DB_HOST=postgres        # service name inside Docker; use localhost for local dev
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=real_estate

# Required only when running the AI repair module
ANTHROPIC_API_KEY=sk-ant-...
